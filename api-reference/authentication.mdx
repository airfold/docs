---
title: 'Authentication'
description: 'Use workspace tokens to authenticate with the API'
icon: 'flag'
---

Retrieve a workspace token with the adaquate permissions from the UI or CLI and use it to authenticate with the API.

```json
"security": [
  {
    "bearerAuth": [] // put your token here
  }
]
```
